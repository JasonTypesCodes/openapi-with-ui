micronaut:
  application:
    name: openapiWithUi
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
  server:
    context-path: services

